import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'app.thecursedapple',
  appName: 'The Cursed Apple',
  webDir: 'dist',
  backgroundColor: '#17110b',
}

export default config
